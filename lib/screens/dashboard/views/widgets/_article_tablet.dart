part of '../../dashboard.dart';

class _ArticleTablet extends StatelessWidget {
  const _ArticleTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Article Table');
  }
}
